cc.Class({
    extends: cc.Component,

    properties: {
    },

    // use this for initialization
    onLoad: function () {
        console.log(this.node)
    },

    // called every frame
    update: function (dt) {

    },
});
